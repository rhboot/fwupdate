prefix=/usr
exec_prefix=/usr
libdir=@@LIBDIR@@
includedir=/usr/include

Name: fwup
Description: Library for deployment of UEFI firmware updates.
Version: @@VERSION@@
Requires: efivar >= 28
Libs: -L${libdir} -lfwup
